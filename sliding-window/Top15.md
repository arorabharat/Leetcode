• Given an array of integers and an integer `k`, find the maximum sum of any contiguous subarray of size exactly `k`.

• Given an array of integers and an integer `k`, find the minimum sum of any contiguous subarray of size exactly `k`.

• Given an array of integers and a window size `k`, return the maximum element in each sliding window of size `k`.

• Given an array of non-negative integers and an integer `k`, find the length of the longest contiguous subarray whose sum is less than or equal to `k`.

• Given an array of non-negative integers and an integer `k`, find the length of the smallest contiguous subarray whose sum is greater than or equal to `k`.

• Given a string, find the length of the longest substring that contains no repeating characters.

• Given a string and an integer `k`, find the length of the longest substring that contains at most `k` distinct characters.

• Given an array of integers and an integer `k`, count the total number of contiguous subarrays that contain at most `k` distinct elements.

• Given an array of integers and an integer `k`, count the total number of contiguous subarrays that contain exactly `k` distinct elements.

• Given two strings `s` and `t`, find the smallest substring in `s` that contains all the characters of `t` including their frequencies.

• Given two strings `s` and `p`, determine whether any permutation of `p` exists as a contiguous substring of `s`.

• Given a string and an integer `k`, find the length of the longest substring that can be obtained by replacing at most `k` characters so that all characters in the substring are the same.

• Given a string and an integer `k`, find the maximum number of vowels in any contiguous substring of size `k`.

• Given a binary array and an integer `k`, find the length of the longest contiguous subarray that contains at most `k` zeros.

• Given an array of positive integers and an integer `k`, count the number of contiguous subarrays whose product is strictly less than `k`.

